import time

my_time = int(input("Geben Sie die Zahl in Sekunden ein: "))

for x in range(my_time, 0, -1):
    seconds = x % 60
    minutes = int(x / 60) % 60
    hours = int(x / 3600)
    print(f"{hours:02}:{minutes:00}:{seconds:30}")
    time.sleep(1)

print("TIME'S UP!")